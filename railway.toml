# Railway configuration — API service
# Deploy via: railway.app → New Project → Deploy from GitHub → select trazabilidad-erp
# Then add PostgreSQL and Redis plugins from the Railway dashboard.

[build]
builder = "dockerfile"
dockerfilePath = "apps/api/Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
